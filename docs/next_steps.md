
Let's discuss about making the Spec Iter more programmatically reliable. My ideas:

1. Basis of all: the support of inline shell output (!`shell command`) make it possible to programmatically generate LLM agent prompts using Python scripts, hence could make some steps or process no longer depend on unreliable agent behaviour.

2. 